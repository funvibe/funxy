// Test: MPTC with HKT
// Transform<F, G> where F: *->*, G: *->*

trait Transform<f, g> {
    fun transform(x: f<Int>) -> g<Int>
}

instance Transform<List, Option> {
    fun transform(x) { Some(1) }
}

print("OK")

