package bugweb

import "lib/json" (Json)

instance Responder Json {
    fun toResponse(j: Json) -> Response {
        // Inline expression - this should now work!
        { status: 200, headers: [], body: BodyJson(j) }
    }
}
