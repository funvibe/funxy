trait Printable<t> {
  fun format(val: t) -> String
}

instance Printable (List<Int>) {
  fun format(val: List<Int>) -> String {
    "List of Ints"
  }
}

l = [1, 2]
print(format(l))

instance Printable (Int, Int) {
  fun format(val: (Int, Int)) -> String {
    "Tuple of Ints"
  }
}
t = (1, 2)
print(format(t))
